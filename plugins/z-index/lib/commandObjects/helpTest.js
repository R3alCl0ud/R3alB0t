module.exports = class help {
    constructor(plugin) {
        this.plugin = plugin;
        this.id = "help"
        this.names = ["help"]
    }
    
    func (bot, message, author, channel, server) {
        
    }
}